import defaultAvatar from "assets/images/default_avatar.png";

export const DEFAULT_AVATAR = defaultAvatar.src;

export const LS_LANG = "LANG";
export const LS_PHOTOS = "MIXTILES_PHOTOS";

export const LANGUAGES = {
  VI: "vi",
  EN: "en",
};

export const SUCCESS = true;
export const ERROR = false;

export const MAX_ATTACHMENT_FILES = 10;
export const MAX_FILENAME_LENGTH = 40;

export const CANVAS_SIZE = 280;
export const FULL_SIZE = 600;
